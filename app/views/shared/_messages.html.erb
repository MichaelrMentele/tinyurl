<% flash.each do |type, message| %>
  <% if type == 'error' then type = 'danger' end %>
  <div class="row">
    <div class="col-sm-offset-4 col-sm-4 alert alert-<%=type%>" role="alert">
      <%= message %>
    </div>
  </div>
<% end %>
